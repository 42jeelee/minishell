pwd는 env의 PWD를 통해 알아내지 않고, getcwd로 알아낸다. ok

cd는 env와 export에 PWD가 있을때는 그 값을 바꾸지만 없다고 새로 만들지는 않는다

//cd 는 env에서 home을 읽어서 home으로 간다.
//cd - 는 env에서 OLDPWD를 읽어서 OLDPWD로 간다.
//cd ~ 는 ~ 로 간다.
//공통: 성공적으로 cd 마쳤을 때
			PWD가 존재하면 PWD에 현재
"cd: OLDPWD not set\n"

1. cd의 플래그를 정한다. CD_COMMON CD_HOME CD_OLD

2. 이동하려는 폴더의 절대경로를 구한다.
	이 때 CD_HOME, CD_OLD의 경우 env에 경로가 없을경우 
		"cd: OLDPWD not set\n" 출력 후 리턴
	이 때 CD_COMMON의 경우 그 경로가 권한이 없으면
		print_perror(cmd[1]) 후 리턴
3. 현재 위치의 절대경로를 구한다.
	
4. 이동하려는 
